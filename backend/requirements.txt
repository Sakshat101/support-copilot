--extra-index-url https://download.pytorch.org/whl/cpu

# --- ML / embedding stack (pinned together; do not loosen) ---
torch==2.8.0
numpy==2.2.6
transformers==5.12.1
sentence-transformers==5.6.0
langfuse<3
# --- web / app ---
fastapi
uvicorn[standard]
langgraph>=1.2,<2
langgraph-checkpoint-postgres
psycopg[binary,pool]>=3.2
pgvector>=0.3
arq>=0.26
redis>=5
openai>=1.40
pydantic-settings>=2
python-dotenv>=1

# --- auth ---
python-jose[cryptography]
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# --- files / misc ---
pymupdf
python-docx
slowapi
python-multipart